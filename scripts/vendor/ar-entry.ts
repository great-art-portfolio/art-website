/** Vendor entry: the exact AR functions the admin flows load at runtime. */
export {
  buildArModels,
  estimateDims,
  resolveDims,
  rebuildArFiles,
  rebuildForDimFix,
  loadImageBlob,
  stemOf,
} from "../../src/lib/ar";
